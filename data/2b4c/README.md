Per-PDB data for 2b4c

Contents:
- edges_combined.npz: graph edge features for chains in this PDB.
- esm_sequences.npz: ESM embeddings / sequence features for residues in this PDB.

Purpose:
- Used for per-structure debugging and inspection; files are also combined into the top-level `data/` NPZs by preprocessing scripts.
