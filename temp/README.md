Temporary workspace

The `temp/` directory holds intermediate and temporary files produced during preprocessing and testing (e.g., `ihd_train_edges.npz`, `ihd_train_esmsequences.npz`, `train_set.csv`).

Files in this folder can be regenerated; it's safe to clear when re-running preprocessing or experiments.
